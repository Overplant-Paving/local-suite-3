You are migrating ONE tool of the Local Suite v2 rebuild (Phase 2, Batch A — zero-network tools). Work only on your assigned tool.

Project root: C:\Users\henry\Developement\Local Suite 2
v1 original (READ-ONLY reference): C:\Users\henry\Developement\Local Suite\{TOOL}.html

READ FIRST (in the project root): MIGRATION.md section 1 (the recipe) and 4 (parity rules), QUALITY.md sections 2 (a11y) and 4 (Definition of Done), ARCHITECTURE.md sections 2-3 (source model, Suite API). Then read core/suite.css, core/suite.js, and tools/focus.html (the reference migration). Then read the v1 original of your tool in full.

DELIVERABLES (exactly these, nothing else):
1. tools/{TOOL}.html — migrated source
2. tests/interactions/{TOOL}.mjs — interaction module for the shared harness
3. tests/evidence/{TOOL}/ — generated by: cd tests && node verify-tool.mjs {TOOL}
4. tests/evidence/{TOOL}/manifest-entry.json
5. tests/evidence/{TOOL}/report.md

HARD RULES:
- Never modify: manifest/tools.json, MIGRATION.md, ROADMAP.md, README.md, dist/, core/, build.py, tests/verify-tool.mjs, tests/smoke.mjs, other tools, or ANYTHING under ..\Local Suite.
- Do not run build.py. Do not run git. Do not commit.
- localStorage keys stay byte-identical to v1. No behavior removed. No features added (a11y attributes excepted, plus any per-tool exception below).

RECIPE (as proven on the pilots):
- Strip from the v1 file: the base-palette variable blocks (--bg --card --ink --muted --line --accent --accent-soft --built --chip --shadow + color-scheme, in all their :root/@media/[data-theme] contexts), the `* { box-sizing...margin:0 }` reset, body font/color/background declarations, `.back` and `.theme-btn` CSS, the base footer styles (core provides margin-top:3rem; font-size:.85rem; border-top:1px solid var(--line); padding-top:1.1rem — add a tool-local override only where v1 differs), `a { color: var(--accent) }`, and the ~9-line theme-toggle script (plus any per-file storage helper IIFE).
- KEEP as tool-local CSS: body padding (and transitions), .wrap width, all layout/components, and any TOOL-SPECIFIC accent variables (e.g. --break) — declared in all four contexts exactly like tools/focus.html.
- Head gets: <link rel="stylesheet" href="../core/suite.css" data-suite-inline>
  Before the tool script: <script src="../core/suite.js" data-suite-inline></script>
- Tool script starts: "use strict"; then Suite.theme.init();
- suite.* storage via Suite.store.get/set (reads v1 bare strings AND JSON; writes strings bare). Legacy non-suite keys (if any) keep raw localStorage access with a comment.
- Convert ALL inline handler attributes (onclick= etc.) AND .onX= property assignments to addEventListener.
- innerHTML with template-literal interpolation: wrap remote/user-influenced expressions in Suite.esc(). For provably-safe local expressions, list the EXACT expression text + one-line reason in report.md under "## escape allowlist requests". Prefer createElement/textContent for new code.
- a11y (QUALITY.md section 2): aria-label on icon-only buttons; Suite.liveRegion() on result containers that update after user actions; <label for=> or aria-label on every input; Enter submits where a text-entry+button pair exists; keyboard path for every mouse path; Esc closes overlays. Note each fix in report.md.

VERIFICATION (mandatory; evidence or it did not happen):
- tests/interactions/{TOOL}.mjs exports:
    export const selectors = [8-12 selectors covering the tool's main components];
    export async function interact({ page, log, evidenceDir }) { /* exercise the CORE FEATURE end-to-end on v2; log() concrete observed values, not assertions of success */ }
    export async function v1Interact({ page }) { /* optional: the same state-writing actions on v1, so localStorage parity compares equal key sets */ }
    export const screenshotAfterInteract = true; // when the interaction changes what is visible
    // export const printShots = true;           // ONLY for print-css tools
  For timer/clock tools use await page.clock.install() FIRST inside interact (and v1Interact).
- Run: cd "C:\Users\henry\Developement\Local Suite 2\tests" && node verify-tool.mjs {TOOL}   — must exit 0.
- Then READ the evidence you produced: computed-style-diff.txt (only justified diffs: -webkit-font-smoothing is pre-approved; content/size differences from dynamic content are fine; anything else fix or explain), localstorage.json (keysOnlyInV1/V2 empty or explained), interaction.txt (console clean), and LOOK at the four screenshots — v2 must be visually indistinguishable from v1 in both themes (few-px shifts tolerated).

tests/evidence/{TOOL}/manifest-entry.json — strict JSON, this schema:
{"id":"{TOOL}","file":"{TOOL}.html","name":<v1 hub name>,"cat":<given below>,"cx":<given below>,"desc":<v1 hub desc, given below>,"network":"offline","key":null,"endpoints":[<every external host the tool can contact — [] unless noted below>],"storage":[<every localStorage key/pattern the tool touches>],"cacheTtlMin":null,"since":"v1","flags":[<given below>]}

tests/evidence/{TOOL}/report.md sections: "## v1 feature walk-through" (every v1 feature as a checklist, each verified — say HOW), "## changes beyond the recipe", "## localStorage keys" (v1 vs v2), "## escape allowlist requests" (exact expressions + reasons, or "none"), "## a11y applied", "## endpoints", "## concerns for the reviewer" (be honest; an empty concerns section with real concerns is a failure).

Your final message: a terse summary — status, feature-walk result, escape requests count, localStorage parity verdict, style-diff verdict, any concerns. The orchestrator reviews your diff against v1 before committing; your "done" claim is not evidence.
