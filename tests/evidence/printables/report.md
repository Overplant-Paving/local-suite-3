# printables.html — migration report (Batch A)

Evidence generated by `node verify-tool.mjs printables` (exit 0). Interaction ran under a
fixed clock (`2026-07-15T10:00`) so the logged months are deterministic.

## v1 feature walk-through

- [x] **Three templates via tabs (month / week / habit)** — clicked all three in the harness;
  interaction.txt lines 1, 9, 12 log each template's rendered `.sh-title` ("July 2026",
  "Week of July 13", "Habit Tracker").
- [x] **Monthly calendar grid** — 35 cells for July 2026, correct lead padding, weekend
  day-numbers greyed, today outline on the 15th (1 today-cell logged; visible in all screenshots).
- [x] **US federal holidays (computed offline, incl. observed shifts)** — July 2026 logs
  "Independence Day (observed)" on Fri Jul 3 + "Independence Day" on Sat Jul 4 (observed-shift
  rule exercised); December 2026 logs "Christmas Day"; Thanksgiving week logs "Thanksgiving".
- [x] **Holiday toggle** — unchecking `#oHol` drops holiday labels to 0 (line 7).
- [x] **Month picker** — `#oMonth` → 2026-12 re-renders "December 2026" (line 4).
- [x] **Week-start Sunday/Monday** — selecting Monday flips the first column header to
  "Monday" (line 5; also visible in v2-after-interaction.png).
- [x] **Optional title (+ month subtitle when titled)** — "Family plan" renders as sh-title
  with "December 2026" demoted to sh-sub (line 6).
- [x] **Weekly planner** — 7 dayblocks with ruled lines, sidebar "Top priorities" / "Notes"
  boxes, week-range subtitle; date picker moves the week (lines 9–11).
- [x] **Habit tracker** — default 5 habits render as rows, 31 day columns for December,
  weekday-letter header row; editing the textarea re-renders rows ("Stretch, Journal")
  (lines 14–15).
- [x] **Auto-orientation** — habit tab auto-switches to landscape (`sheet.landscape=true`,
  `@page { size: landscape; margin: 12mm; }` logged); returning to month restores portrait
  (lines 13, 16).
- [x] **Paper select (portrait/landscape)** — manual select updates the `#pageStyle` @page rule
  and the `.sheet.landscape` class (line 8).
- [x] **Jump to today** — resets month/weekDate to the (fixed) current date; oMonth reads
  2026-07 (line 17).
- [x] **Print button** — `window.print` stubbed in the harness (headless dialog would block);
  click invoked it (line 18).
- [x] **Print CSS (the product)** — `@media print` block and the `#pageStyle` @page element
  preserved byte-for-byte from v1 (they reference no stripped theme vars). Compared
  v1-print.png vs v2-print.png side by side: identical — chrome/panel/footer hidden via
  `.noprint`, sheet loses border/shadow/padding, calendar cells grow to 108px, holidays and
  today-outline print.
- [x] **Always-light printable sheet** — `.sheet` hardcodes white/ink; dark-theme screenshots
  show the white sheet on the dark page in both versions.
- [x] **Theme toggle** — harness probe: light → dark, `aria-pressed=true`, persisted to
  `suite.theme`.

## changes beyond the recipe

- `o()` helper gained an optional third `forId` argument so every generated `<label>` carries
  `for=` (a11y; markup otherwise identical). All call sites updated; the checkbox already had
  `for="oHol"` in v1.
- `#sheet` wrapped in `Suite.liveRegion()` (a11y).
- Nothing else: layout CSS, sheet CSS, print CSS, holiday math, and all render functions are
  verbatim v1. v1's local `escHtml`/`escAttr` helpers kept as-is (offline tool, no remote data;
  swapping them for `Suite.esc` would change escaping behavior for no benefit).

## localStorage keys

| key | v1 | v2 |
|---|---|---|
| `suite.theme` | written by theme toggle | written by `Suite.theme` (core) |

No other keys in either version. localstorage.json: `keysOnlyInV1: []`, `keysOnlyInV2: []`.

## escape allowlist requests

All interpolations are local, offline-computed values; user-typed values go through v1's own
escapers. If the `--check` heuristic flags these, they are verified clean:

- `escAttr(state.habits)`, `escAttr(state.title)` — user input, escaped for attribute/text context by v1's `escAttr` (escapes `& < > "`).
- `escHtml(state.title)`, `escHtml(hol)`, `escHtml(h)` — escaped by v1's `escHtml` (`& < >`; text-node context, no attributes).
- `state.month`, `state.weekDate` — values of `<input type=month|date>`; browser-constrained to `YYYY-MM(-DD)` shape, and invalid input yields `""` which the change handler discards.
- `MONTHS[monthIdx]`, `DOW_LONG[d]`, `DOW_LTR[dow]`, `year`, `day`, `d`, `daysInMonth` — literals from const arrays / numbers.
- `rng`, `start.toLocaleDateString(...)`, `dt.toLocaleDateString(...)` — locale date strings from the Date API.
- `(state.weekStart===0?" selected":"")` etc., `(state.holidays?" checked":"")`, `(wknd ? "wknd " : "")`, `(key === todayKey ? "today" : "")`, `(tab==="week"?"My week":...)` — literal-only ternaries.

## a11y applied

- Every generated form control now has an associated `<label for=>` (oMonth, oWeekDate,
  oHabitMonth, oHabits, oWeekStart, oTitle, oOrient; oHol had one in v1).
- Preview container `#sheet` is `aria-live="polite"` via `Suite.liveRegion` (re-renders on
  every option change).
- Theme button: `aria-label` + `aria-pressed` from core `Suite.theme.init()`.
- No icon-only buttons (Print button has text alongside the 🖨 glyph); no overlays (no Esc
  path needed); all controls are native buttons/inputs/selects so the keyboard path matches
  the mouse path; no search-box/button pair (Enter n/a — inputs re-render live).

## endpoints

None. Fully offline; no fetch calls anywhere in the file.

## concerns for the reviewer

- **Print fidelity is asserted via `emulateMedia("print")` screenshots**, not a real printed
  PDF. The @media print rules and @page element are byte-identical to v1, and the emulated
  print screenshots match exactly, but Chromium's print-preview pagination (@page
  size/margins) isn't exercised by the harness.
- The harness's non-interaction screenshots (v1/v2 light, dark, print) run **without** the
  fixed clock, so they show the real current month — today that is also July 2026, making
  them coincidentally identical to the fixed-clock month. Both versions are captured in the
  same run, so the v1/v2 comparison is valid on any date.
- `window.print` was stubbed for the print-button click (headless dialog would hang the
  harness); the stub only proves the listener fires, not the dialog.
- `DOW_SHORT` and `todayY` are declared but unused — true in v1 as well; kept for parity.

## Phase 4 a11y audit

QUALITY.md §2 re-verified against `tools/printables.html` from `file://`, light + dark, on
all three sheet types (raw log: `phase4-a11y-audit.txt`). **Verdict: fixed (sheet grays).**

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| 1 | icon-only controls named | pass (n/a) | none |
| 2 | async regions aria-live | pass | `#sheet` = polite — the preview is the single result surface for every option change |
| 3 | keyboard paths | pass | keyboard-only drive: Enter on "Weekly planner" tab → weekly sheet, typed a date → "Week of June 28 · June 28 – July 4, 2026", typed a custom title (live retitle), **Space toggled** the holidays checkbox (markers re-rendered), Enter on "Habit tracker" → grid + auto-landscape; habits textarea reachable. Print is a labelled `<button>` → `window.print()` (not invoked in the audit). No traps. Chrome's month input takes 2 Tabs for its MM/YYYY segments then exits — on the second segment the author outline is replaced by Chrome's native black focus ring (screenshot-verified visible; scanner's NO-INDICATOR line is a false positive) |
| 4 | input labels | pass | all generated option controls get `label[for]` from the `o()` builder incl. the checkbox |
| 5 | contrast both palettes | **fixed** | see below |
| 6 | focus visibility | pass | outline/native ring on every stop, both themes |

Contrast fixes (the sheet is always-white by design — print parity — so single values):
- weekend day numbers `table.cal .wknd .dnum` #999→**#757575**: was **2.85** → 4.6.
- weekly planner date `.dayblock .dh span` #888→**#757575**: was **3.54** → 4.6.
- habit weekday letters `table.habit thead th.wd` #999→**#757575**: was **2.85** → 4.6.
Other sheet grays measured and passing: `th` #555 = 7.46, `.sh-sub` #666 = 5.74, `.hol`
#b23 = 6.2.

Harness: `node verify-tool.mjs printables` re-run after the fix — exit 0 (print-media
screenshot regenerated), console clean.
SUITE-WIDE flags: muted-on-`--bg` 4.36 light (footer); white-on-accent 2.36 dark (`.tabs
button.on`, `.btn`).
